# CPM Session State

**Skill**: cpm:consult
**Phase**: Consultation in progress
**Topic**: /cpm:ralph epic plan execution — preventing formal plan mode in autonomous loops
**Active agents**: Bella
**Lead**: user

## Discussion Highlights

### Key points so far
- **Problem**: /cpm:ralph loops stall when `/cpm:do` hits a `[plan]`-tagged story and calls `EnterPlanMode`, creating an interactive approval gate
- **Root cause**: `/cpm:do` has no awareness of Ralph/autonomous mode. The ralph prompt says "use inline planning" but the SKILL.md instruction to call `EnterPlanMode` wins
- **Rejected approach**: Adding autonomous mode detection to `/cpm:do` (reads `.claude/ralph-loop.local.md`) — works but adds permanent complexity to a mostly-interactive skill
- **Chosen approach**: `/cpm:ralph` strips `[plan]` tags from epic docs during pre-flight, before launching the loop
  - Fits naturally after Step 1a (epic discovery), before Step 2 (prompt assembly)
  - Tags are execution hints, not specification — stripping doesn't change what gets built
  - Log stripped tags to the ralph execution log for audit trail and transparency
  - Reversible via git if needed

### Decisions
1. Strip `[plan]` tags in ralph pre-flight — **confirmed by Chris**
2. Log what was stripped (option 2) — **confirmed by Chris**

### Active thread
Decision is made. Ready for implementation or further discussion.

## Next Action
Chris may want to draft the exact changes to `cpm/skills/ralph/SKILL.md` or continue discussing.
