# CPM Session State

**Skill**: cpm:consult
**Phase**: Consultation in progress
**Topic**: Making exit phrasing consistent between /cpm:party and /cpm:consult
**Active agents**: Bella
**Lead**: user

## Discussion Highlights

### Key points so far
- Party mode was updated to use "wrap up" instead of "exit"/"quit" because those are reserved by Claude Code CLI and cause session termination + orphaned progress files
- Consult mode still uses "exit", "quit" as trigger words — needs the same fix
- Key differences identified in consult SKILL.md that need updating:
  1. "Present Exit Option" prompt still says "Type **exit** to end the consultation"
  2. No warning about CLI reserved words
  3. Exit Handling triggers list includes "exit" and "quit"
  4. Check for Commands section lists "exit", "done", "end", "quit", "goodbye"
  5. Pipeline Handoff last option says "Just exit" instead of "Done for now"

### Active thread
Starting consultation with Bella about making these changes consistent.

## Next Action
Continue the consultation. The user wants to discuss making exit phrasing consistent. Lead is user. Active agents: Bella. Resume the conversation loop.
