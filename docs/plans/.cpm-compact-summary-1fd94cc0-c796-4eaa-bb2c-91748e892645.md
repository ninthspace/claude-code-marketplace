# Compact Summary (PostCompact)
**Captured**: 2026-07-25 13:24:31
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Session context**: This session resumed from a compacted prior conversation. The carried-over instruction was "Let's do" — run `/cpm:do` through the epics of spec 41 (CPM's pivot from hand-generated HTML to Claude artifacts). The organising principle established with the user before compaction: *an HTML file that merely mirrors a Markdown file earns nothing; a complementary interpretation is worth making.*

**User messages in this session (post-compaction)**: There were exactly two user turns, both the single word "Continue". Everything else was tool results, system reminders, or AskUserQuestion responses (which are tool results, not user turns).

**Chronological work**:

1. **Resumed mid-Task 1.1 of epic 41-03 Story 1** — placing the canonical reference line at seven remaining skills. I had already located placement sites via fence-boundary analysis (discover 142, brief 210, architect 233, spec 311) and needed to find review/audit/retro output sections.

2. Found the sections: review `### Step 3: Write Review File` (175), audit `### Step 3: Deliverable Generation` (250), retro `### Step 3: Write Retro File` (83).

3. Placed the canonical line at all seven, removed the superseded line from `Faithful Render (on request)` in architect/review/spec in the same edits. Verified: `grep -rh ... | sort | uniq -c` → 1 unique string, count 10.

4. Created `cpm/hooks/tests/test-reference-line-propagation.sh` (21/21) with a fence-aware `enclosing_heading` helper and four negative controls.

5. Verified Story 1, recorded verification + retro in epic doc, marked coverage rows 1/2/2b ✓.

6. Hydrated Story 2 (tasks 86-89). Wrote nine skill-specific "For `{skill}` the artifact is …" sentences plus the verbatim heuristic. Created `test-skill-artifact-guidance.sh` (51/51).

7. End-to-end read at the gate found three prose defects: `discover`'s "which…which" clause, `audit`'s "top ten" vs spec's "maximum 10", and the heuristic reading as a false gate at `status`/`epics`. All three fixed.

8. Wrote retro 17 (`docs/retros/17-retro-per-skill-artifact-guidance.md`).

9. **User: "Continue"** → Epic 41-04 (Retire the Mirrors). Ran the retro consumption gate with four observations (retros 16, 17, 15, 14) — all four dispositions returned "Applied".

10. Story 1: deleted the three Faithful Render sections from spec/architect/review. End-to-end read found `architect`'s frontmatter still advertised "an on-request HTML render" — fixed. Created `test-faithful-render-retired.sh`. Initially 15/17 — two failures because I expected the markdown table row count to be 3 (header + separator + body) when `grep -c '^| '` doesn't match the `|---|---|` separator line. Fixed to 2, and the negative-control fixture check to `-ne 2`. Then 17/17.

11. Story 2: captured baseline (22 suites, all green). Deleted `test-faithful-render.sh`, removed `check_render_path`, `check_communication_path`, `check_counts_agree` from `html-test-helpers.sh`. Updated a stale comment on `check_uses_shared_template`. Found and fixed one dangling comment reference in `test-present-artifact-pivot.sh`. Reworked `test-dashboard-export.sh` onto a body fragment (19/15 → corrected to 15/15 by collapsing paired asserts). Created `test-suite-prune.sh` — initially 19/22 because the suite matched itself in its own sweep; fixed with `--exclude="$(basename "$0")"`. Then 22/22.

12. Discovered the suite-count criterion was arithmetically unsatisfiable (22 − 1 + 1 = 22) — recorded as inline change.

13. Story 3: verified companion-asset carve-out. `do/SKILL.md` untouched, `epics` mockup block byte-identical to HEAD. Added five durable assertions to `test-companion-assets.sh` (31/20 → 36/25), each negative-controlled.

14. Wrote retro 18 (`docs/retros/18-retro-retire-mirrors.md`).

15. **User: "Continue"** → Epic 41-05 (Documentation and Release). Retro gate with four observations (retros 18, 14, 17, 18) — all "Applied", including "Applied — consolidate in this epic" for the heuristic wordings.

16. Premise check found: cpm/README.md had NO faithful-render text at all; the real drift was `status` claiming "no files created or modified" and `epics` never mentioning the dependency view. Also found README's file tree listed 18 of 21 skills.

17. Story 1: updated cpm/README.md (present, status, epics, artifact sections + file tree + assets/shared entries). Updated `cpm-training-guide.html` SKILLS records (present, status, epics, artifact). Consolidated the heuristic to one wording. Created `test-docs-artifact-pivot.sh` (19/19).

18. Verified Story 1, recorded inline changes + retro, marked coverage rows 1/2 ✓.

19. Story 2 hydrated (tasks 106-109). Task 2.1 survey found **six** live version sites (vs three last release), all at 3.0.0. Found both presentation decks carried stale "HTML dashboard" claims — fixed both. Applied the 3.1.0 bump to all six sites. Verified.

20. Marked task 107 complete, 108 in_progress. **This is where the summary request arrived** — I was about to write the release-bump test suite.

**Key technical details to preserve**:
- The canonical reference line's exact bytes
- The heuristic's exact bytes
- Test framework counting behaviour (test_start once, each assert separately)
- The six version sites
- The `enclosing_heading` fence-aware awk pattern
- Standing constraints (no cache writes, no bulk sed edits, no AgentTool/workflows, version control stays with user)

**Errors I made and fixed**:
- Markdown table row count expectation (3 vs 2) in test-faithful-render-retired.sh
- Self-matching sweep in test-suite-prune.sh
- Inherited count inflation in test-dashboard-export.sh (19/15 → 15/15)

**Design of the pending test suite** (task 108): I had established that `test-audit-skill.sh` already covers semver shape + manifest agreement, so the new suite should cover documentation badges agreeing with the manifest, plus the must-NOT sweep — implemented as "read version from manifest, grep tests for that literal" (derived, not pinned).
</analysis>

<summary>
1. **Primary Request and Intent**

   The session continues a `/cpm:do` run through the epic chain of **spec 41** (`docs/specifications/41-spec-artifact-pivot.md`) — CPM's pivot from hand-generated HTML to Claude artifacts. The organising principle, established with the user before compaction: *an HTML file that merely mirrors a Markdown file earns nothing; a complementary interpretation is worth making.*

   Two user turns in this session, both the single word **"Continue"** — the first advancing from epic 41-03's completion to 41-04, the second from 41-04's completion to 41-05.

   Standing constraints in force throughout:
   - **NEVER read or write files in `~/.claude/plugins/cache/ninthspace-marketplace/`** — that directory holds installed copies, is overwritten on updates, and changes there are lost and untracked by git. **ALWAYS use the source files in this repository.** (Exception: a running skill reading its own SKILL.md from cache is normal runtime behaviour.)
   - **No bulk programmatic edits** — never `sed`/`perl`/`awk` to edit files; Edit tool only, file-by-file.
   - *"Do not call the AgentTool unless the user requested it"*; *"Do not use workflows or deep-research unless the user requested it."*
   - **Version control stays with the user** — no commits, staging, branching, or pushing. Nothing has been committed this session.

2. **Key Technical Concepts**

   - **CPM (Claude Planning Method) plugin**, 21 skills, source in `cpm/`. Repo also holds `noteplan`, `php-lsp`, `js-simplifier`, `filament-mockup`.
   - **cpm:do work loop**: retro consumption gate (per-observation disposition via AskUserQuestion) → story hydration (TaskCreate per `###` task + a verification gate blocked by them) → per-task workflow → Step 8 batch summary (epic verification, proof recording, retro generation, report, next-epic check).
   - **Byte-identical propagation pattern** (retro 11/12): one shared section + a byte-identical one-line reference at each use site, verified by `grep -rh … | sort | uniq -c` which proves count and identity together, each paired with one skill-specific sentence.
   - **Shell test framework** (`cpm/hooks/tests/test-helpers.sh`): `test_start` increments `TESTS_RUN` once; `test_pass`/`test_fail` increment per assert — so multiple asserts under one `test_start` inflates the ratio (e.g. "19/15 passed"). Helpers: `test_start`, `test_pass`, `test_fail`, `assert_contains`, `assert_not_contains`, `assert_empty`, `assert_equals`, `test_summary`, `TEST_TMPDIR` (mktemp -d, trapped on EXIT).
   - **`run-all-tests.sh` discovers suites by glob** (`for test_file in "$SCRIPT_DIR"/test-*.sh`), excludes `test-helpers.sh`, holds no manifest.
   - **Artifact tool contract**: supplies its own `<!doctype html>`/`<head>`/`<body>` so content must be a **body fragment**; strict CSP blocks external hosts but permits inline CSS/JS; requires loading `artifact-design` first; `favicon` required; same file path redeploys to same URL.
   - **Test tag vocabulary**: `[unit]`, `[integration]`, `[feature]`, `[manual]`, `[tdd]`, `[plan]`.
   - **Markdown table row counting gotcha**: `grep -c '^| '` does not match the `|---|---|` separator, so a one-role table counts **2** (header + body), not 3.

3. **Files and Code Sections**

   **The canonical reference line (exact bytes propagated to all ten skills)**:
   ```
   An artifact can be published from this output on request — follow the shared **Artifact Publishing** procedure. It is always separately confirmed, and never the default.
   ```
   Defined in a fenced block at `cpm/shared/skill-conventions.md:475`. Now at exactly 10 sites in `cpm/skills/`: `discover:144`, `brief:214`, `architect:235`, `spec:311`, `epics:361`, `review:239`, `audit:258`, `retro:91`, `status:140`, `present:116`.

   **The conservative heuristic (exact bytes, now the only wording — 11 occurrences across 9 skills)**:
   ```
   if you cannot write the one-line justification for what the visual carries that the prose cannot, it has not earned its place
   ```

   **`cpm/hooks/tests/test-reference-line-propagation.sh` (created, 21/21)** — Story 41-03.1's tests. Key helper:
   ```bash
   # enclosing_heading <file> <lineno> — the nearest preceding Markdown heading that is
   # not inside a fenced code block. Template fences in these skills contain `##`
   # headings of their own, so fence tracking is what makes the answer meaningful.
   enclosing_heading() {
     awk -v L="$2" '
       /^```/ { fence = !fence; next }
       !fence && /^#+ / && NR < L { h = $0 }
       NR == L { print h }
     ' "$1"
   }
   ```
   Also `ref_lines`, `variant_lines` (`ref_lines "$1" | grep -vxF "$CANONICAL"`), `line_of_canonical`.

   **`cpm/hooks/tests/test-skill-artifact-guidance.sh` (created, 51/51)** — Story 41-03.2's tests. `NINE="discover brief architect spec epics review audit retro status"`; asserts each carries `For \`$skill\` the artifact is`, the heuristic verbatim, the guidance at exactly `ref_ln + 2`, plus the autonomous-run rule at its single shared home: `grep -qF 'autonomous runs (\`cpm:ralph\`) never publish'`.

   **The nine skill-specific sentences written** (Story 41-03.2): problem map (`discover`), value-proposition canvas (`brief`), architecture explorer (`architect`), requirement explorer (`spec`), readiness picture (`epics`), findings explorer by severity (`review`), nine-dimension findings dashboard (`audit`), trend view across retros (`retro`), full project picture (`status`). At `status`/`epics` the heuristic is framed as *"That justification is also the test for anything *else* the page/view might carry — as with companion assets, …"*.

   **`cpm/hooks/tests/test-faithful-render-retired.sh` (created, 17/17)** — Story 41-04.1's tests. Asserts absence behaviourally (heading AND write path), paired with presence assertions for the Companion Assets sections; asserts no skill frontmatter advertises a render.

   **`cpm/hooks/tests/test-suite-prune.sh` (created, 22/22)** — Story 41-04.2's tests:
   ```bash
   REMOVED="check_render_path check_communication_path check_counts_agree"
   SURVIVING="check_self_contained check_source_unchanged check_valid_html check_valid_fragment check_asset_path check_reference_resolves check_uses_shared_template check_section_contains check_valid_json"
   ```
   The caller sweep excludes itself: `assert_empty "$(grep -rn "$v" "$TESTS_DIR" --exclude="$(basename "$0")")"`.

   **`cpm/hooks/tests/test-dashboard-export.sh` (reworked, 15/15)** — fixture re-pointed from a `template.html`-built document to a body fragment at `$TEST_TMPDIR/docs/plans/status-artifact-full-picture.html`, validated by `check_valid_fragment`. Dropped `check_uses_shared_template`/`check_valid_html`; kept payload well-formedness, inline-JS-only, no write-back, source immutability. Added: fragment-vs-document negative control, and a guard that the handler uses `if(navigator.clipboard)` and never `permissions.query`.

   **`cpm/hooks/tests/html-test-helpers.sh` (modified)** — removed `check_render_path`, `check_counts_agree`, `check_communication_path` with their comment blocks; rewrote `check_uses_shared_template`'s preamble to name only the surviving companion-asset role. Nine validators remain.

   **`cpm/hooks/tests/test-companion-assets.sh` (extended, 36/25)** — five R6 carve-out guards appended, each negative-controlled against stripped copies:
   ```bash
   test_start "cpm:do still treats a companion asset as a visual design target"
   if grep -qF 'visual design target' "$DO_SKILL" \
     && grep -qF 'Do **not** parse the companion HTML to extract requirements' "$DO_SKILL"; then
   ```
   plus `cpm:do reaches companion assets by relative path, not URL`, the `epics` markup-oracle rule, the convention's not-published rule + rationale, and the storage path.

   **`cpm/hooks/tests/test-docs-artifact-pivot.sh` (created, 19/19)** — Story 41-05.1's tests. Every assertion is a **pairing** (retired claim absent AND replacing claim present). File-tree check derives the expected value:
   ```bash
   LISTED=$(sed -n '/^├── skills\/$/,/^├── tools\/$/p' "$README" \
     | grep -oE '^│   [├└]── [a-z]+/' | grep -oE '[a-z]+' | sort)
   ACTUAL=$(ls "$SCRIPT_DIR/../../skills" | sort)
   ```
   Plus: README states no skill count, no version literal; heuristic has exactly one wording via `sort -u`.

   **`cpm/README.md` (modified)** — `present` output line dropped the `.html` sibling; `status` section rewritten (read-only scan, full-picture publish offered-not-automatic, register row as the one durable write, new example line); `epics` gained a dependency-view paragraph and example; `artifact` section qualified the backlink claim; file tree repaired from 18→21 skills plus `assets/` and `shared/` entries.

   **`cpm-training-guide.html` (modified)** — `SKILLS` array records updated for `present` (no `.html` companion), `status` (Phase 4, register row, full picture), `epics` (dependency view in `tip`), `artifact` (backlink qualified). Array still parses; 21 records, all with `output`/`outputFile`.

   **`cpm-presentation.html` / `cpm-onboarding-presentation.html` (modified)** — both dropped the stale "HTML dashboard" promise for `/cpm:status`; both version badges bumped.

   **Version bump to 3.1.0 applied at all six surveyed sites**:
   - `README.md:108` — `### Claude Planning Method (v3.1.0)`
   - `cpm-onboarding-presentation.html:468` — title-badge
   - `cpm-presentation.html:456` — title-badge
   - `cpm-training-guide.html:376` — version-badge
   - `.claude-plugin/marketplace.json:46` — `plugins[cpm].version`
   - `cpm/.claude-plugin/plugin.json:4` — `"version"`

   Remaining `3.0.0` hits are **historical records that must NOT be rewritten**: `docs/epics/40-04-*`, `docs/retros/14-*`, `docs/plans/.cpm-compact-summary-*`.

   **Retros created**: `docs/retros/17-retro-per-skill-artifact-guidance.md`, `docs/retros/18-retro-retire-mirrors.md`.

   **Epic docs updated**: 41-03 (Complete), 41-04 (Complete), 41-05 (In Progress) — each with `**Retro applied**` breadcrumbs, `**Inline change**` breadcrumbs, `**Verification**` blocks and story-level `**Retro**:` lines; coverage matrices marked ✓.

   **Progress file** `docs/plans/.cpm-progress-1fd94cc0-c796-4eaa-bb2c-91748e892645.md` was deleted after epic 41-03 finished.

4. **Errors and fixes**

   - **`test-faithful-render-retired.sh` initially 15/17**: I expected the convention's one-role storage table to count 3 rows (`header + separator + body`). `grep -c '^| '` does not match `|---|---|` — the correct count is **2**. Fixed the assertion to `-eq 2` with an explanatory comment, and the negative-control fixture check to `-ne 2`. Then 17/17.
   - **`test-suite-prune.sh` initially 19/22**: the suite matched *itself* in its caller sweep, because it must name the removed validators to test for them and its fixture deliberately calls one. Fixed with `--exclude="$(basename "$0")"` and a comment stating why.
   - **`test-dashboard-export.sh` reported 19/15 after rework**: inherited count inflation from four `test_start` blocks each carrying two asserts. Collapsed each into a single `if … test_pass else test_fail` claim → 15/15.
   - **Stale comment reference**: `test-present-artifact-pivot.sh:7` named `check_communication_path` in a historical comment after the validator was deleted. Rewritten so a reader grepping the symbol doesn't find a phantom.
   - **Test title/assertion mismatch**: the title said "naming cpm:ralph" but the assertion only checked two separate substrings. Tightened to `grep -qF 'autonomous runs (\`cpm:ralph\`) never publish'`.
   - No user corrections were issued in this session; both user turns were "Continue".

5. **Problem Solving**

   - **Placement bound outside `Faithful Render`** (epic 41-03, coverage row 2b): replacing the superseded line in place would have satisfied 41-03's gate and silently dropped three of ten sites when 41-04 deleted those sections, with nothing failing at either gate. The line was instead placed against each skill's primary saved output. Confirmed at 41-04's gate: `test-reference-line-propagation.sh` still reports one unique string ×10 unmodified.
   - **Fence-aware structural queries**: every CPM SKILL.md keeps `##` headings inside its output-format code fence, so nearest-preceding-heading scans return template headings (`## Dependencies`, `### Unit Testing`) that are fiction. Fence tracking is the default requirement, not an edge case.
   - **Criterion unsatisfiable arithmetic** (41-04 Story 2): "one fewer suite than baseline" ignores that Task 2.4 adds a suite (22 − 1 + 1 = 22). Verified via observable equivalents instead, recorded in both epic and coverage docs.
   - **Retro 16's rework premise was wrong**: it said 41-04 would prune `check_uses_shared_template`, but that validator keeps a live caller in `test-companion-assets.sh` since companion assets retain the shared template under R6. The suite needed reworking for a different reason (its fixture modelled a retired document shape).
   - **`git diff` criteria have a one-commit shelf life**: Story 41-04.3's guard was converted into five durable assertions.
   - **Premise check found the criterion pointing at the wrong defect** (41-05): `cpm/README.md` had no faithful-render text at all; the real drift was `status` claiming "no files created or modified" (false since a confirmed publish appends a register row) and `epics` never mentioning the dependency view.
   - **Six version sites, not three**: the last release found three; the survey found six (two presentation decks and the training guide additionally). All agreed at 3.0.0 beforehand, so nothing was already stale this time.

6. **All user messages**

   - **"Continue"** (advancing from epic 41-03 to 41-04)
   - **"Continue"** (advancing from epic 41-04 to 41-05)

   (AskUserQuestion selections are tool results, not user turns. Those recorded: epic 41-04 gate — "Applied" ×4 for retros 16·Scope, 17·Patterns, 15·Testing, 14·Testing; epic 41-05 gate — "Applied" ×3 plus "Applied — consolidate in this epic" for retro 17·Codebase.)

   Standing constraints restated verbatim so they survive compaction: *"NEVER read or write files in the plugin cache directory (`~/.claude/plugins/cache/ninthspace-marketplace/`). That directory contains installed copies of plugins and is overwritten on updates. Changes made there are lost and not tracked by git. ALWAYS use the source files in this repository."* · *"Do not call the AgentTool unless the user requested it"* · *"Do not use workflows or deep-research unless the user requested it"* · no bulk `sed`/`perl`/`awk` edits, Edit tool only · version control stays with the user.

7. **Pending Tasks**

   - **Task 108 (in_progress)**: write tests for the release bump — Story 41-05.2, Task 2.3.
   - **Task 109 (pending, blocked by 106/107/108)**: Story 2 verification gate, "Verify: Release 3.1.0".
   - After that: epic 41-05 completion (mark Status Complete, coverage rows 3/4/5 ✓, write retro 19), which closes spec 41's five-epic chain.

8. **Current Work**

   I had just completed **Task 2.2** (applied the 3.1.0 bump to all six surveyed sites, verified each reads `3.1.0`, confirmed the only remaining `3.0.0` hits are historical records under `docs/`), marked task 107 completed, and set **task 108 to in_progress**.

   Before writing the suite I had already established its design constraints by inspecting the existing coverage:
   - `cpm/hooks/tests/test-audit-skill.sh` **already covers** semver shape and manifest-to-manifest agreement (repaired in epic 40-04 per retro 14). Its helper: `manifest_version() { awk -F'"' '/"version"[[:space:]]*:/ {print $4; exit}' }`; it checks `plugin.json` matches `^[0-9]+\.[0-9]+\.[0-9]+$` and that `marketplace.json`'s cpm block agrees.
   - A naive `[0-9]+\.[0-9]+\.[0-9]+` sweep for the must-NOT **false-positives** on `test-reference-line-propagation.sh:30` ("Task 41-01.2.3"). The robust design I settled on: **read the current version from the manifest, then grep the test files for that literal** — derived, not pinned, which is exactly retro 14's invariant-not-snapshot rule.

   The remaining gap for the new suite to cover: every documentation badge/heading version agreeing with the manifest (derived), and the must-NOT sweep for pinned version literals in test assertions, with negative controls.

   Epic 41-05's story-2 criteria verbatim:
   - `grep -rn` for the current version string is run **before** any edit, and its full result set is recorded [integration]
   - Every site returned by that grep carries `3.1.0` afterwards [integration]
   - Cross-manifest agreement is asserted structurally — semver shape and manifest-to-manifest equality — rather than by comparing against a pinned literal [integration]
   - must NOT pin a version literal in any test assertion [integration]

9. **Optional Next Step**

   Write `cpm/hooks/tests/test-release-version.sh` covering Story 41-05.2's criteria (Task 2.3, task #108, currently in_progress), then run it and `run-all-tests.sh`, then close the Story 2 verification gate (task #109).

   The task's description verbatim: *"Write automated tests covering the story's acceptance criteria tagged `[unit]`, `[integration]`, or `[feature]`."* — with Task 2.2's own description setting the standard it must meet: *"Retro 14's testing gap was two assertions pinning the literal `2.0.0`, stale since that release and failing silently because nobody runs the suite between releases. Assert the invariant, not the snapshot."*

   The suite should assert: the six documentation sites' versions agree with `cpm/.claude-plugin/plugin.json` (value derived from the manifest, never pinned); no file under `cpm/hooks/tests/` contains the current version literal; plus negative controls. After that, record the Story 2 verification block and retro in `docs/epics/41-05-epic-documentation-and-release.md`, mark coverage rows 3/4/5 ✓ in `docs/epics/41-05-coverage-documentation-and-release.md`, set the epic Status to Complete, and write `docs/retros/19-retro-documentation-and-release.md`.
</summary>
