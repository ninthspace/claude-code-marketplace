# Compact Summary (PostCompact)
**Captured**: 2026-07-20 12:49:42
**Trigger**: auto

<analysis>
Let me work through this conversation chronologically.

**Initial invocation**: The user invoked `/cpm:consult` with Bella as the agent, asking to amend the cpm board tool to treat epics that are Superseded or Withdrawn as Complete for filtering/counting, and to check that "Done" status is properly accounted for (a screenshot showed a board where "hide completed" still showed Done epics, and a Saloon v4 epic reading "0/1" with Status: Done).

**Consult phase (as Bella)**: I investigated the code. Key findings:
- The board hard-codes exact-match `"Complete"` in ~10 places across `status_model.py` and `board_view.py`.
- CPM canonical vocab is Pending/In Progress/Complete.
- The `pivot` skill already accepts "done"/"complete" case-insensitively.
- Crucially, mid-consult I discovered Chris had ALREADY implemented Superseded/Withdrawn as an epic-level "retired/inactive" concept that EXCLUDED such epics from counts (opposite of "everything counts"). This contradicted his stated intent.

Through the consult, Chris made these decisions:
1. "everything counts, regardless of how it's deemed complete" → count as done (flip exclude→count)
2. Superseded/Withdrawn epic-level only
3. Normalise: Done/Superseded/Withdrawn all mean the same as Complete
4. Dependencies: Superseded/Withdrawn PERMANENTLY block dependents (their work never done); Done satisfies deps like Complete
5. Retro nudge: retired epics get NO retro (→ archive); Done epics do get retro

The consult produced discussion record `docs/discussions/26-discussion-terminal-status-counting.md`.

**First /cpm:quick (record 21)**: Implemented the "count terminal statuses as done" change. Files changed: status_model.py (added `_DONE_STATUSES`, `_is_done`, `_epic_is_terminal`, flipped derive_project count, used `_is_done` in derive_state/_epic_state/_deps_satisfied), board_view.py (added Done to _STORY_STYLE, used _is_done in _progress/_is_in_progress/visible_stories), test_derivation.py (inverted test, added Done + dependency + no-retro tests), shared/status-model.md (documented count model + dependency split), and 5 skills (status/do/ralph/archive/epics) to accept Done. 191 tests passed. Completion record: `docs/quick/21-quick-board-count-terminal-statuses-spec.md`.

**Question about story completion criteria**: User asked precise criteria. I explained `_is_done`: status stripped+lowercased in {"complete","done"}, with epic-terminal override for progress counting, and dependency satisfaction being story's own `_is_done`.

**Noise question**: User showed a real status "Folded into Story 10 — delivered as part of the PanelDeliberationPage conversion (kept here for triage traceability; do not execute separately)" — should be complete, wondering whether to change the toolset to clean up such noise. I recommended against prose-inference, offered options via AskUserQuestion. User picked "Lint/surface only" (don't parse prose, flag unrecognised statuses so human fixes source). I confirmed story-level Superseded/Withdrawn should be flagged as unrecognised.

**Second follow-up**: User said "yes, we should then be able to implement option 2 as part of the tool (so I can update docs manually or have the /cpm skills do it)" — meaning ALSO implement lead-token parsing (Option 2). Then via AskUserQuestion on skills scope, user picked "Teach do/pivot to preserve notes".

**Second /cpm:quick (record 22, IN PROGRESS)**: Implementing lead-token status parsing + lint/surface unrecognised statuses + teaching do/pivot to preserve notes. Spec file: `docs/quick/22-quick-board-status-lead-token-lint-spec.md`.

Work completed so far in this quick:
- status_model.py: Added `_STATUS_NOTE_RE`, `_status_token`, `_STORY_STATUSES`, `_EPIC_STATUSES`, `_INACTIVE_LOWER`, made `_is_done`/`_is_pending`/`_is_in_progress` token-based, added `_is_recognised_status`, updated `_is_inactive`, `_unblocked_pending`, `derive_state`, `_epic_state`, `_blocking_deps` to use token predicates.
- board_view.py: Updated imports (removed INACTIVE_EPIC_STATUSES import, added `_is_inactive`/`_is_pending`/`_is_recognised_status`/`_status_token`, aliased `_is_in_progress as _status_in_progress`), replaced `_STORY_STYLE` dict with `_story_style()` function + warning constants (`_WARN_GLYPH="⚠"`, `_WARN_MARKER="(!)"`, `_WARN_STYLE="bold red"`), rewrote `_is_in_progress`, added `_epic_has_unrecognised`, updated `epic_rows` (uses `_is_inactive`, appends `(!)` marker), updated `blocking_rows`, added `unrecognised_rows()`, rewrote `story_rows` to flag unrecognised.
- board.py: Updated `_epic_detail_content` to prepend unrecognised preface.
- Ran tests: 191 passed (after code changes, before new tests).
- test_derivation.py: Just added new tests section for lead-token parsing & unrecognised lint.

Still pending: test_board_view.py new tests (story ⚠ flag, epic (!) marker, unrecognised_rows), docs (contract status-model.md lead-token convention, status skill callout, do/pivot note preservation), run tests, Step 4 verification + completion record, delete progress file.

The most recent action was adding tests to test_derivation.py via Edit (succeeded). I was about to add tests to test_board_view.py.

Let me capture the security/behavioral constraints: CLAUDE.md rules — never read/write plugin cache dir for dev work (use repo source cpm/); never use `git -C`; commit to current branch, never auto-branch; never finalize decisions on user absence. The Skill runtime reads from cache (that's normal).

Let me note the exact test additions I just made and the pending test_board_view additions I had planned (drafted in my thinking): test_story_row_flags_unrecognised_status, test_epic_row_flags_unrecognised_status_with_marker, test_unrecognised_rows_names_bad_statuses, test_lead_token_story_is_not_flagged.

Let me note the test helpers: test_board_view.py has `epic(path, *, status="In Progress", stories=(), title="", blocked_by="—")` at line 112, `status(state, *, actions=(), epics=(), complete=0, total=0, name="proj")` at line 26, imports from board_view at lines 13-22 (need to add `unrecognised_rows`), and `from status_model import Epic, NextAction, ProjectStatus, State, Story` at line 23.

Test command: `cd cpm/tools/board && uv run pytest`.
Numbering: quick record 22 (active max 20 → 21 used → next 22). Discussion record was 26.
</analysis>

<summary>

1. Primary Request and Intent:
   The user (Chris) is developing CPM plugins in a marketplace repo and progressively refined the cpm "board" tool's handling of story/epic statuses through three linked pieces of work:
   - **Initial (/cpm:consult with Bella)**: Amend the cpm board tool to treat Superseded/Withdrawn epics as Complete for filtering and counting, AND verify "Done" status is properly accounted for (a screenshot showed the "hide completed" toggle still showing Done epics, and a Done epic reading "0/1"). Through consult, decisions locked: (1) everything counts (flip from exclude→count); (2) Superseded/Withdrawn are epic-level only; (3) Done/Superseded/Withdrawn all mean the same as Complete for counting/state/hiding; (4) Superseded/Withdrawn PERMANENTLY block dependents while Done satisfies deps like Complete; (5) retired epics get no retro nudge (→ archive), Done epics still get retro.
   - **/cpm:quick record 21 (COMPLETE)**: Implemented decisions above.
   - **Question then feature request**: User asked precise criteria for a story being "complete". Then showed a real noisy status ("Folded into Story 10 — delivered as part of the PanelDeliberationPage conversion (kept here for triage traceability; do not execute separately)") and asked whether to change the toolset to clean up such noise. User chose (via AskUserQuestion) "Lint/surface only" — don't parse prose, flag unrecognised statuses; confirmed story-level Superseded/Withdrawn should be flagged as unrecognised.
   - **Latest feature (/cpm:quick record 22, IN PROGRESS)**: User then said to ALSO implement "option 2" = lead-token status parsing (read the leading status token before a delimiter, ignore/preserve the tail as a human note), "so I can update docs manually or have the /cpm skills do it". Via AskUserQuestion on skills scope, user chose "Teach do/pivot to preserve notes."

2. Key Technical Concepts:
   - CPM plugin suite (cpm/) with skills (SKILL.md files) and a Python "board" TUI tool (Textual/Rich).
   - Status model: Pending / In Progress / Complete (canonical); Done (read-tolerant synonym); Superseded/Withdrawn (epic-level terminal "retired" statuses).
   - "Read tolerant, write strict": CPM writes `Complete`; readers accept `Done`.
   - Two distinct predicate sets: **done-for-counting** = {Complete, Done, Superseded, Withdrawn}; **satisfies-dependency** = {Complete, Done} only (retired epics permanently block).
   - Lead-token parsing: canonical status = token before first delimiter (`—`, `–`, `(`, `;`, spaced hyphen); tail is a preserved human note.
   - Lint/surface: unrecognised statuses flagged (⚠ on story row, (!) on epic row, epic-detail preface, /cpm:status callout), never silently counted; unrecognised still counts as not-done (conservative).
   - /cpm:quick workflow: startup checks → classify (fix/change) → scope → propose+confirm (AskUserQuestion) → write spec file (hard gate) → execute → verify+completion record → delete progress file.
   - Test runner: `cd cpm/tools/board && uv run pytest`.
   - Numbering: per-type, max(active ∪ archived)+1, min 2-digit zero-pad.

3. Files and Code Sections:

   - **cpm/tools/board/status_model.py** (heavily modified across both quicks):
     - Added lead-token infrastructure (record 22):
       ```python
       _STATUS_NOTE_RE = re.compile(r"\s*[—–(;]|\s+-\s+")
       def _status_token(status: str) -> str:
           return _STATUS_NOTE_RE.split(status.strip(), maxsplit=1)[0].strip()
       _STORY_STATUSES = {"pending", "in progress", "complete", "done"}
       _EPIC_STATUSES = _STORY_STATUSES | {"superseded", "withdrawn"}
       _DONE_STATUSES = {"complete", "done"}
       INACTIVE_EPIC_STATUSES = {"Superseded", "Withdrawn"}
       _INACTIVE_LOWER = {s.lower() for s in INACTIVE_EPIC_STATUSES}
       def _is_done(status): return _status_token(status).lower() in _DONE_STATUSES
       def _is_pending(status): return _status_token(status).lower() == "pending"
       def _is_in_progress(status): return _status_token(status).lower() == "in progress"
       def _is_recognised_status(status, *, epic=False):
           token = _status_token(status).lower()
           if not token: return True
           return token in (_EPIC_STATUSES if epic else _STORY_STATUSES)
       ```
     - `_is_inactive` now token-based: `return _status_token(epic.status).lower() in _INACTIVE_LOWER`
     - `_epic_is_terminal(epic)`: `return _is_inactive(epic) or _is_done(epic.status)`
     - `derive_project` count (record 21): `total = sum(len(e.stories) for e in epics)`; `complete = sum(len(e.stories) if _epic_is_terminal(e) else sum(1 for s in e.stories if _is_done(s.status)) for e in epics)`
     - derive_state/_epic_state use `_is_pending`/`_is_in_progress`/`_is_done` (renamed `complete`→`done` list); `_unblocked_pending` uses `not _is_pending(story.status)`; `_deps_satisfied` uses `not _is_done(target.status)`/`not _is_done(target_epic.status)`; `_blocking_deps` uses `_is_pending(s.status)`.

   - **cpm/tools/board/board_view.py** (modified both quicks):
     - Imports updated: removed `INACTIVE_EPIC_STATUSES`, added `_is_inactive, _is_pending, _is_recognised_status, _status_token`, plus `from status_model import _is_in_progress as _status_in_progress` (avoids clash with local `_is_in_progress(epic)`).
     - Replaced `_STORY_STYLE` dict with:
       ```python
       _WARN_GLYPH = "⚠"
       _WARN_MARKER = "(!)"
       _WARN_STYLE = "bold red"
       def _story_style(status):
           if _is_done(status): return "green"
           if _status_in_progress(status): return "yellow"
           return ""
       ```
     - `_is_in_progress(epic)` rewritten token-based.
     - Added `_epic_has_unrecognised(epic)` (checks epic status + all stories).
     - `epic_rows`: uses `_is_inactive(epic)` for reference-row choice; appends `(!)` marker post-pass: `if row.epic is not None and _epic_has_unrecognised(row.epic): row.label = f"{row.label}  {_WARN_MARKER}"`.
     - `blocking_rows`: `_is_pending(s.status)`.
     - Added `unrecognised_rows(epic)` returning `[(label, style)]` naming each bad status, header line prefixed with ⚠.
     - `story_rows` rewritten: unrecognised → `⚠ Story N · title · <raw status>` with `_WARN_STYLE`; recognised → shows `_status_token(story.status)` with `_story_style(story.status)`.

   - **cpm/tools/board/board.py**: `_epic_detail_content` rebuilt to prepend `board_view.unrecognised_rows(row.epic)` then (if blocked) `blocking_rows`, with a blank-line spacer between, before rendering the doc markdown.

   - **cpm/tools/board/tests/test_derivation.py**: (record 21) inverted `test_retired_epic_stories_count_as_done` (asserts (3,4)), added `test_retired_epic_does_not_nudge_a_retro`, `test_done_story_counts_toward_progress`, `test_all_done_stories_resolve_to_complete_state`, `test_dependency_on_done_epic_is_satisfied`, `test_dependency_on_retired_epic_stays_permanently_blocked`. (record 22, JUST ADDED) new section with `test_lead_token_status_counts_as_done`, `test_lead_token_applies_to_pending`, `test_unrecognised_status_is_not_counted`, `test_status_token_and_recognition`. Helper: `epic_md(epic_status="Pending", stories=(), blocked_by="—")`.

   - **cpm/tools/board/tests/test_board_view.py**: NOT YET MODIFIED (record 22). Helpers: `epic(path, *, status="In Progress", stories=(), title="", blocked_by="—")` (line 112), `status(state, *, actions=(), epics=(), complete=0, total=0, name="proj")` (line 26). Imports from board_view at lines 13-22 (need to add `unrecognised_rows`); `from status_model import Epic, NextAction, ProjectStatus, State, Story`. Existing Superseded test (lines 159-176) and story_rows test (251-259) verified to still pass with changes.

   - **cpm/shared/status-model.md**: (record 21) documented count model, Done read-tolerant rule, dependency split, retired-epics section. (record 22) NOT YET updated for lead-token convention.

   - **cpm/skills/{status,do,ralph,archive,epics}/SKILL.md**: (record 21) updated to accept Done as Complete synonym; status skill flipped "excluded from counts" → "count as done". Note: archive/SKILL.md was also modified by user/linter (Signals 1 and 4 accept Done). (record 22) status skill callout, do/pivot note-preservation NOT YET done.

   - **docs/discussions/26-discussion-terminal-status-counting.md**: created (consult record).
   - **docs/quick/21-quick-board-count-terminal-statuses-spec.md**: completion record (COMPLETE).
   - **docs/quick/22-quick-board-status-lead-token-lint-spec.md**: spec file (Status: Confirmed — awaiting execution), needs promotion to completion record.
   - **docs/plans/.cpm-progress-02093cf5-8361-456e-b012-7eea4b06bdfa.md**: active progress file (Step 2/3), must be deleted at end.

4. Errors and fixes:
   - Early zsh glob error: `grep --include=*.md` failed with "no matches found" (zsh tried to expand `*.md`). Fixed by quoting: `--include='*.md'`.
   - Discovered mid-consult that Chris's uncommitted working tree contradicted his stated "everything counts" intent (his code EXCLUDED retired epics). I surfaced this contradiction explicitly rather than proceeding, and Chris resolved it (count, don't exclude). This was the key "don't assume" moment.
   - Name-clash risk: board_view.py has a local `_is_in_progress(epic)` and status_model.py now has `_is_in_progress(status)`. Resolved by importing status_model's as `_status_in_progress`.
   - No test failures encountered; 191 tests passed after record-21 changes and again after record-22 code changes (before new tests).

5. Problem Solving:
   - Distinguished two predicate sets (done-for-counting vs satisfies-dependency) — the crux insight that Done ≠ Superseded/Withdrawn on dependency resolution.
   - Kept lead-token + lint composing cleanly: `Complete — note` parses to Complete; `Folded into...` (no status word) flagged.
   - Ensured lead-token convention applies to ALL status predicates (pending/in-progress/done) for consistency, not just done.
   - Verified existing tests survive (all fixtures use recognised statuses).

6. All user messages:
   - "[/cpm:consult] bella, we need to amend the cpm board tool to treat epics that are either Superseded or Withdrawn as Complete as far as filtering and counting is concerned. Can we also check that Done status is properly accounted for (for example this screenshot which hides completed epics etc.) still shows epics that are done [Image #1]"
   - "everything counts, regardless of how it's deemed complete."
   - "yeah, update /cpm:status too. i've just updated the /cpm toolset to know about superseded and withdrawn states, so you should check before updating"
   - "1. No, Superseded and Withdrawn should permanently block anything that depends on it because that work will never be done. 2. Yes"
   - "that's good let's wrap up and /cpm:quick"
   - "what's the precise criteria for a story being deemed complete as far as the tool is concerned"
   - "here's an example status of a project that i'm developing: [status: Folded into Story 10 — delivered as part of the PanelDeliberationPage conversion (kept here for triage traceability; do not execute separately)] This should be deemed complete. I'm wondering whether i should change the cpm toolset to 'clean up' such noise"
   - (AskUserQuestion answer) "Lint/surface only"
   - "yes, we should then be able to implement option 2 as part of the tool (so I can update docs manually or have the /cpm skills do it)"
   - (AskUserQuestion answer) "Teach do/pivot to preserve notes"
   - Security/behavioral constraints from CLAUDE.md / memory (verbatim intent): NEVER read or write files in the plugin cache directory (`~/.claude/plugins/cache/ninthspace-marketplace/`) for development work — always use repo source under cpm/ (exception: a running skill reads its own SKILL.md from cache, which is normal). Never use `git -C <path>` — run git from within the repo dir. On commit/`/commitp`, commit to the currently checked-out branch; never auto-branch, even on main, unless asked. Never assume an answer or finalize a decision that is the user's to make just because they're away/timed out. Nothing has been committed (per user's standing no-auto-commit preference).

7. Pending Tasks:
   - Finish /cpm:quick record 22 execution:
     - Add tests to test_board_view.py (story ⚠ flag, epic (!) marker, unrecognised_rows, lead-token-not-flagged).
     - Update cpm/shared/status-model.md to document the lead-token convention + linting.
     - Update cpm/skills/status/SKILL.md to add the unrecognised-status callout.
     - Update cpm/skills/do/SKILL.md and cpm/skills/pivot/SKILL.md so status transitions preserve an existing `— note` tail (make explicit that prefix-scoped Edit already preserves it; guard against clobbering).
     - Run `cd cpm/tools/board && uv run pytest` to verify.
     - Step 4: verify acceptance criteria against docs/quick/22-quick-board-status-lead-token-lint-spec.md, write completion record (in place, Status→Complete), delete progress file.
   - Offered earlier (awaiting user): stage/commit all uncommitted changes when ready (user has not yet said to commit).

8. Current Work:
   Immediately before this summary I was executing Step 3 of /cpm:quick record 22. I had just successfully added a new test section to `cpm/tools/board/tests/test_derivation.py` (via Edit, anchored before the `# --- contract conformance` header) containing `test_lead_token_status_counts_as_done`, `test_lead_token_applies_to_pending`, `test_unrecognised_status_is_not_counted`, and `test_status_token_and_recognition`. All board code changes (status_model.py, board_view.py, board.py) were complete and 191 tests passed before adding these new tests. I was about to add the corresponding tests to `test_board_view.py`.

9. Optional Next Step:
   Add tests to `cpm/tools/board/tests/test_board_view.py` (add `unrecognised_rows` to the board_view import block), covering: a story row flagged with ⚠ + `_WARN_STYLE` ("bold red") for an unrecognised status; an epic row label ending with "(!)" when a story is unrecognised; `unrecognised_rows()` naming bad statuses but not recognised ones; and a lead-token status ("Complete — folded into 10") being deemed done/hidden and not flagged. Then run `cd cpm/tools/board && uv run pytest`, then proceed to the docs updates (status-model.md lead-token convention, status skill callout, do/pivot note preservation), then Step 4 verification and completion record for `docs/quick/22-quick-board-status-lead-token-lint-spec.md`, and delete the progress file. This directly continues the confirmed record-22 spec whose acceptance criteria include: "A story-level Superseded/Withdrawn is flagged as unrecognised" and "`/cpm:do` and `/cpm:pivot` prose states that a status transition preserves an existing `— note` tail" and "The `cpm/tools/board` test suite passes."
</summary>
