# Coverage Matrix: Skill Reporting Sites

**Source spec**: docs/specifications/50-spec-report-disposition.md  
**Epic**: docs/epics/50-02-epic-skill-reporting-sites.md  
**Date**: 2026-08-16

> **Verification rule**: Verification status (✓) is bound to criterion text. Any change to a story criterion or its spec mapping resets that row to unverified.

| # | Spec Requirement | Spec Text (verbatim) | Story Criterion (verbatim) | Covered by | Spec Test Approach | Verified |
|---|---|---|---|---|---|---|
| 1 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `do` Step 8's epic summary is derived from the coverage rows, story observations and change-moment resolutions rather than narrated beside them | Story 1 | `[unit]` | ✓ |
| 2 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `do`'s change-moment resolutions are reported with their disposition | Story 1 | `[unit]` | ✓ |
| 3 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `quick` Step 4 reports each criterion's disposition derived from its tri-state `met` | Story 2 | `[unit]` | ✓ |
| 4 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `review` reports each finding's disposition derived from whether it carries a `remediation_task_id` | Story 2 | `[unit]` | ✓ |
| 5 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `pivot` Phase 4 reports affected tasks with their disposition | Story 2 | `[unit]` | ✓ |
| 6 | FR6 | Where a disposition is already a row, the report is derived from those rows. Five sites: `do` Step 8 and its change moments, `quick` Step 4's tri-state `met`, `review`'s findings and `remediation_task_id`, `pivot` Phase 4, `audit`'s findings. | `audit` reports each finding with its disposition | Story 2 | `[unit]` | ✓ |
| 7 | FR6 (must NOT) | Where a disposition is already a row, the report is derived from those rows. | must NOT retain an instruction to summarise alongside the rows without deriving from them at any of the five row-backed sites. Control: removing the derivation sentence from any one site fails that site's test | Story 4 | `[unit]` | ✓ |
| 8 | FR7 | `do`'s autonomous mode uses this vocabulary rather than its own. Its standalone "surface the two sets separately" instruction is the same rule invented once for one case. | `do`'s autonomous section references the shared vocabulary and its standalone "surface the two sets separately" phrasing is gone — both halves asserted | Story 1 | `[unit]` | ✓ |
| 9 | FR8 | `inspect`, `archive` and `clean` adopt the labels. They hold no disposition rows, so they take FR1–FR5 without FR6. Both `clean` and `archive` already carry private proto-dispositions. | `inspect` Step 6 names the `disposition` domain and reports its findings with their disposition | Story 3 | `[unit]` | ✓ |
| 10 | FR8 | `inspect`, `archive` and `clean` adopt the labels. They hold no disposition rows, so they take FR1–FR5 without FR6. Both `clean` and `archive` already carry private proto-dispositions. | `archive`'s "Report what was stamped and what was skipped" is expressed as Fixed and Left alone | Story 3 | `[unit]` | ✓ |
| 11 | FR8 | `inspect`, `archive` and `clean` adopt the labels. They hold no disposition rows, so they take FR1–FR5 without FR6. Both `clean` and `archive` already carry private proto-dispositions. | `clean`'s "Report what was deleted, what was left" is expressed as Fixed and Left alone | Story 3 | `[unit]` | ✓ |
| 12 | FR1 (must NOT) | The set is closed: a report uses these or omits the item. | must NOT define a disposition term privately in any skill file. Control: `clean:86` and `archive:136` fail this before the change | Story 4 | `[unit]` | ✓ |
| 13 | FR10 | The four dispositions are seeded `taxonomy` rows readable via `list_taxonomy` with `domain: 'disposition'`, inserted if absent so an existing database gains them on next open. | `do` names the `disposition` domain rather than listing the four labels | Story 1 | `[unit]` | ✓ |
| 14 | FR10 (must NOT) | The four dispositions are seeded `taxonomy` rows readable via `list_taxonomy` with `domain: 'disposition'`, inserted if absent so an existing database gains them on next open. | must NOT hardcode a disposition label string in any skill file. Control: a fixture skill file carrying one fails the sweep | Story 4 | `[unit]` | ✓ |
| 15 | FR4 | Needs-you items come last, together, each an imperative naming what to do and where. Fixed, Left alone and Unverified precede it in that order, so the reader can stop early having missed nothing actionable. | One real report from each of `do`, `quick` and `clean`: the reader can stop after the third block having missed nothing actionable | Story 4 | `[manual]` | ✓ |
