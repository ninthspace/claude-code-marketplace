# Coverage Matrix: Execution Skill Behavioural Edits

**Source spec**: docs/specifications/40-spec-opus-5-alignment.md
**Epic**: docs/epics/40-02-epic-execution-skill-behaviour.md
**Date**: 2026-07-24

> **Verification rule**: Verification status (✓) is bound to criterion text. Any change to a story criterion or its spec mapping resets that row to unverified.

| # | Spec Requirement | Spec Text (verbatim) | Story Criterion (verbatim) | Covered by | Spec Test Approach | Verified |
|---|---|---|---|---|---|---|
| 1 | R2 — Verification triage | "Artefact-producing verification is retained unchanged: the story verification gate, its acceptance-criteria pass/fail record in the epic doc, test-command execution for tagged criteria, coverage-matrix proof recording, and the Step 5b retest-after-refactor check." | "The Step 5b **Retest** block, its revert-on-failure branch, and the `reverted` simplifier outcome are retained unchanged" | Story 1 | `[manual]` | ✓ |
| 2 | R2 — Step 5b retest exemption | "Step 5b's third precondition skips the entire refactoring pass when no test command is cached precisely because the retest is the only check catching a behaviour-changing refactor." | "Step 5b's third precondition is retained unchanged, including its rationale citing the retest" | Story 1 | `[manual]` | ✓ |
| 3 | R2 — Step 5b retest exemption | "Removing it would delete a safety net, not scaffolding. The retest stays." | "An inline note records that the retest was assessed under R2's artefact-production test and exempted, so a future alignment pass can tell a deliberate retention from an oversight" | Story 1 | `[manual]` | ✓ |
| 4 | Architecture Decision — Verification triage by artefact-production test | ""Output written into an artefact" includes a step whose result drives control flow — a revert, a skip, a branch — not only one that writes prose into a document." | "must NOT weaken any verification whose output is written to an artefact or drives a revert" | Story 1 | — | ✓ |
| 6 | R2 — Verification triage | "epic-level re-verification against the source spec" | "Step 8 item 1's epic-level verification against the source spec is removed" | Story 2 | `[manual]` | ✓ |
| 7 | R2 — Verification triage | "Each removal carries a one-line inline rationale" | "The removal carries a one-line inline rationale" | Story 2 | `[manual]` | ✓ |
| 8 | R2 — Verification triage | "Artefact-producing verification is retained unchanged: … coverage-matrix proof recording." | "**Epic-level proof recording** is retained — remaining unverified coverage-matrix rows are still marked `✓` — with its trigger re-anchored now that the verification it was gated on is gone" | Story 2 | `[manual]` | ✓ |
| 9 | — *(story-originated)* | — | "The retro-trigger flag set's "epic-level spec gap" entry (Step 8 items 2 and 3) is resolved consistently with the removal" | Story 2 | — | ✓ |
| 10 | R2 — Verification triage | "must NOT weaken any verification whose output is written to an artefact" | "must NOT weaken any verification whose output is written to an artefact" | Story 2 | `[manual]` | ✓ |
| 11 | Architecture Decision — Verification triage by artefact-production test | "Retain any verification step whose output is written into an artefact a human or a downstream skill reads. Remove any verification step whose only consumer is the model's own confidence." | "Every verification site in `quick/SKILL.md` and `epics/SKILL.md` is classified by the artefact-production test: retained when its output is written into an artefact a human or downstream skill reads, removed when its only consumer is the model's own confidence" | Story 3 | — | ✓ |
| 12 | R2 — Verification triage | "and any "confirm/re-check before reporting" phrasing" | "Any "confirm/re-check before reporting" phrasing found is removed with a one-line inline rationale" | Story 3 | `[manual]` | ✓ |
| 13 | R2 — Verification triage | "Artefact-producing verification is retained unchanged: the story verification gate, its acceptance-criteria pass/fail record in the epic doc, test-command execution for tagged criteria, and coverage-matrix proof recording." | "`quick`'s Step 4 criterion-by-criterion assessment, its test execution, and the completion record's `## Acceptance Criteria` and `## Verification` sections are retained unchanged" | Story 3 | `[manual]` | ✓ |
| 14 | R2 — Verification triage | "Artefact-producing verification is retained unchanged: … coverage-matrix proof recording." | "`epics`' Step 3d coverage matrix construction and Step 4 cross-epic gap check are retained unchanged" | Story 3 | `[manual]` | ✓ |
| 15 | — *(story-originated)* | — | "A no-change outcome for either skill is acceptable, recorded with its reasoning rather than left as a silent skip" | Story 3 | — | ✓ |
| 16 | R6 — Scope constraint | "Add an explicit scope-constraint instruction to both skills" | "Both `quick` and `do` carry the scope-constraint instruction in positive prose" | Story 4 | `[manual]` | ✓ |
| 17 | R6 — Scope constraint | "deliver what was asked at the scope intended, make routine judgment calls without checking in, raise a better approach in a sentence and continue rather than silently transforming the task" | "The instruction states: deliver what was asked at the scope intended, make routine judgment calls without checking in, raise a better approach in a sentence and continue rather than silently transforming the task" | Story 4 | `[manual]` | ✓ |
| 18 | R6 — Scope constraint | "`quick` is the priority — bypassing pipeline ceremony for small changes is its entire purpose." | "`quick` carries it where it governs Step 1 classification and Step 3 execution — bypassing pipeline ceremony for small changes is its entire purpose" | Story 4 | `[manual]` | ✓ |
| 19 | R6 — Scope constraint | "must NOT conflict with `do`'s existing inline-change breadcrumb behaviour" | "must NOT conflict with `do`'s existing inline-change breadcrumb behaviour" | Story 4 | `[manual]` | ✓ |
| 20 | Integration Boundaries — Verification gate → coverage matrix | "the gate's pass/fail output is the contract feeding coverage-matrix proof recording. R2 must not disturb it; this is the boundary the artefact-production test exists to protect." | "The story verification gate's pass/fail output still feeds coverage-matrix proof recording unchanged — the gate → matrix contract is intact end to end" | Story 5 | — | ✓ |
| 21 | R2 — Verification triage | "Artefact-producing verification is retained unchanged: the story verification gate, its acceptance-criteria pass/fail record in the epic doc, test-command execution for tagged criteria, and coverage-matrix proof recording." | "All four artefact-producing verifications R2 names as retained are present: the story verification gate, its acceptance-criteria pass/fail record in the epic doc, test-command execution for tagged criteria, and coverage-matrix proof recording" | Story 5 | `[manual]` | ✓ |
| 22 | Maintainability NFR | "Every deletion under R2 and R10 carries an inline rationale." | "Every removal across Stories 2–3 carries an inline rationale" | Story 5 | — | ✓ |
| 23 | — *(story-originated)* | — | "R6's scope constraint does not contradict R2's retained verification steps in either skill" | Story 5 | — | ✓ |

## Notes

- **Pivot applied 2026-07-24 — rows 1–4 replaced, row 5 retired.** `/cpm:pivot` on R2 resolved the Step 5b retest tension in favour of retention: the retest's output drives a revert of the working tree and is recorded in the `**Simplifier outcomes**:` line, so R2's own artefact-production test exempts it. R2 now lists the retest among its retained verifications, and the spec's architecture decision gained a "Reading the rule" clause covering control-flow outputs. Story 1 was re-scoped from removal to retention and its criteria dropped from five to four — the row numbering keeps its gap at 5 rather than renumbering, so references to rows 6–23 stay stable. No `✓` markers were cleared: every row was unverified at the time of the pivot.
- **Rows 9, 15, 23** — story-originated. All are cascade-handling for the remaining removals: the spec specifies *what to delete* without naming what the deletions break. Confirmed with Chris on 2026-07-24.
- **Rows 4, 11, 20, 22** — trace to architecture decisions, an integration boundary, and an NFR rather than a numbered requirement, so the spec's Acceptance Criteria Coverage table assigns them no tag.
