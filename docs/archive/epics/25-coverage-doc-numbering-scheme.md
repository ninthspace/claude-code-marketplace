# Coverage Matrix: Document Numbering Scheme

**Source spec**: docs/specifications/28-spec-doc-numbering-scheme.md
**Epic**: docs/epics/25-epic-doc-numbering-scheme.md
**Date**: 2026-04-11

> **Verification rule**: Verification status (✓) is bound to criterion text. Any change to a story criterion or its spec mapping resets that row to unverified.

| # | Spec Requirement | Spec Text (verbatim) | Story Criterion (verbatim) | Covered by | Spec Test Approach | Verified |
|---|---|---|---|---|---|---|
| 1 | M1 | Every skill that creates a numbered document applies the same rule via a shared "Numbering" procedure: zero-pad to minimum 2 digits, grow naturally past 99 using natural width, integer comparison for the next-number lookup. | The CPM shared skill conventions doc contains a "Numbering" section with the canonical rule `[manual]` | Story 1 | `[manual]` | ✓ |
| 2 | M1 | Every skill that creates a numbered document applies the same rule via a shared "Numbering" procedure: zero-pad to minimum 2 digits, grow naturally past 99 using natural width, integer comparison for the next-number lookup. | The procedure specifies integer comparison (not lexical) for the next-number lookup `[manual]` | Story 1 | `[manual]` | ✓ |
| 3 | M1 | Each skill references the shared procedure rather than restating the rule inline. | Each of the nine affected skills replaces its inline "find highest number" paragraph with "Follow the shared Numbering procedure." verbatim `[manual]` | Story 2 | `[manual]` | ✓ |
| 4 | M1 | Applies to `/cpm:spec`, `/cpm:epics`, `/cpm:discover`, `/cpm:brief`, `/cpm:review`, `/cpm:retro`, `/cpm:architect`, `/cpm:quick`, and discussion-save logic in `/cpm:party` and `/cpm:consult`. | Skills affected: `/cpm:spec`, `/cpm:epics`, `/cpm:discover`, `/cpm:brief`, `/cpm:review`, `/cpm:retro`, `/cpm:architect`, `/cpm:quick`, and discussion-save logic in `/cpm:party` and `/cpm:consult` `[manual]` | Story 2 | `[manual]` | ✓ |
| 5 | M1 | `/cpm:spec` invoked in a directory containing a synthetic `99-spec-foo.md` produces `100-spec-bar.md` on the next invocation — not `99-spec-bar.md`, not an error. | `/cpm:spec` invoked in a directory containing `99-spec-foo.md` produces `100-spec-bar.md` on the next invocation (fixture scenario) `[manual]` | Story 2 | `[manual]` | ✓ |
| 6 | M2 | Creating skills look at both `docs/{type}/` and `docs/archive/{type}/` when computing the next number. New number = `max(active ∪ archived) + 1`. Retired numbers are never reused. | The shared Numbering procedure specifies globbing both `docs/{type}/` and `docs/archive/{type}/` when computing max `[manual]` | Story 1 | `[manual]` | ✓ |
| 7 | M2 | `/cpm:spec` with `27-spec-foo.md` in the archive and nothing in the active directory produces `28-spec-bar.md`, not `01-spec-bar.md`. | `/cpm:spec` with `27-spec-foo.md` in the archive and nothing in the active directory produces `28-spec-bar.md`, not `01-spec-bar.md` (fixture scenario) `[manual]` | Story 2 | `[manual]` | ✓ |
| 8 | M2 | If `docs/archive/{type}/` does not exist (fresh project, nothing ever archived), the lookup degrades cleanly and uses the active directory only. | The procedure degrades cleanly when `docs/archive/{type}/` does not exist (fresh project) — falls back to active-only lookup `[manual]` | Story 1 | `[manual]` | ✓ |
| 9 | M3 | `/cpm:epics` creates new epics with the pattern `{parent}-{seq}-epic-{slug}.md`, where `{parent}` is the spec number the epic derives from and `{seq}` is a sub-sequence number starting at `01` within that parent. | `/cpm:epics` invoked from `docs/specifications/28-spec-foo.md` produces epic files matching `28-01-epic-{slug}.md`, `28-02-epic-{slug}.md`, ... `[manual]` | Story 3 | `[manual]` | ✓ |
| 10 | M3 | Coverage-matrix companion files inherit the same prefix: `{parent}-{seq}-coverage-{slug}.md`. | Companion coverage files use the same two-part prefix: `28-01-coverage-{slug}.md` `[manual]` | Story 3 | `[manual]` | ✓ |
| 11 | M3 | Sub-numbers are immutable identifiers — gaps from deletions are preserved, never backfilled. | Creating a new epic under spec 27 when the directory contains `27-01-epic-foo.md` and `27-03-epic-baz.md` produces `27-04-epic-qux.md` — gap at 27-02 preserved via max-lookup over [27-01, 27-03]. Top-of-sequence deletions are not preserved without archive (retired numbers remain visible via archive-mirror glob). `[manual]` | Story 3 | `[manual]` | ✓ |
| 12 | M3 | (follows from AD3 scoped glob definition) | The scoped glob `docs/epics/{parent}-[0-9]*-epic-*.md` ignores flat-shape files when computing the next sub-number `[manual]` | Story 3 | `[manual]` | ✓ |
| 13 | M4 | Epics with no parent spec (from `/cpm:quick`, discussion-driven work, or standalone invocation) use `00` as the parent prefix: `00-{seq}-epic-{slug}.md`. | An epic created without a parent spec is written as `00-{seq}-epic-{slug}.md` `[manual]` | Story 3 | `[manual]` | ✓ |
| 14 | M4 | (follows from M4 scoping rule) | The "find next sub-number" logic for orphans scopes to `00-[0-9]*-epic-*.md` independent of spec-linked epics `[manual]` | Story 3 | `[manual]` | ✓ |
| 15 | M4 | All new epics produced by `/cpm:epics` use the two-part format — no new flat-shape epics are written. | No new flat-shape epic (e.g. `28-epic-{slug}.md`) is ever written by the updated `/cpm:epics` `[manual]` | Story 3 | `[manual]` | ✓ |
| 16 | M5 | `/cpm:epics`, `/cpm:do`, and any skill that reads the epics directory handles both shapes transparently. | `/cpm:epics` invoked in a directory containing both `15-epic-consult.md` and `27-01-epic-foo.md` reads both without error `[manual]` | Story 3 | `[manual]` | ✓ |
| 17 | M5 | The "find next sub-number" logic scopes to two-part filenames only; flat-shape files are naturally excluded. | The general epic glob `docs/epics/[0-9]*-epic-*.md` is preserved for operations needing all epics regardless of shape `[manual]` | Story 3 | `[manual]` | ✓ |
| 18 | M5 | Existing flat-shape epics are not migrated, renamed, or touched. | No flat-shape epic file is renamed, moved, or modified by the updated `/cpm:epics` `[manual]` | Story 3 | `[manual]` | ✓ |
| 19 | M6 | `/cpm:do` resolves both flat and two-part epic paths when hydrating a task, and resolves the matching coverage matrix file via the `-epic-` → `-coverage-` substitution rule, which works for both shapes. | `/cpm:do` hydrating a task from `docs/epics/27-01-epic-foo.md` resolves the epic doc and `docs/epics/27-01-coverage-foo.md` correctly `[manual]` | Story 4 | `[manual]` | ✓ |
| 20 | M6 | `/cpm:do` resolves both flat and two-part epic paths when hydrating a task. | `/cpm:do` hydrating a task from `docs/epics/15-epic-bar.md` (flat) resolves the epic doc and `docs/epics/15-coverage-bar.md` correctly `[manual]` | Story 4 | `[manual]` | ✓ |
| 21 | M6 | (follows from AD4 substitution rule) | The coverage-path resolution uses the single `-epic-` → `-coverage-` substitution rule (AD4) — one code path for both shapes `[manual]` | Story 4 | `[manual]` | ✓ |
| 22 | M7 | The shared Numbering procedure explicitly states the invariant: "Numeric prefix is an integer identifier, not a fixed-width string. Never pad existing files to match new widths." | The shared Numbering procedure contains the invariant statement verbatim: "Numeric prefix is an integer identifier, not a fixed-width string. Never pad existing files to match new widths." `[manual]` | Story 1 | `[manual]` | ✓ |
| 23 | M7 | The `/cpm:epics` skill contains a one-line transition note explaining the coexistence: "Epics created before the numbering update used flat `{nn}-epic-{slug}.md`; new epics use parent-scoped `{parent}-{seq}-epic-{slug}.md`. Both shapes coexist; old epics are not migrated." | The `/cpm:epics` skill contains a one-line transition note explaining the coexistence `[manual]` | Story 3 | `[manual]` | ✓ |
| 24 | S2 | `/cpm:epics` template guidance on `**Blocked by**: Epic {...}` lines demonstrates both `15-epic-foo` (legacy) and `27-01-epic-foo` (new) shapes as acceptable, so authors don't assume one shape is required. | Cross-epic dependency reference guidance in the skill template shows both `15-epic-foo` (legacy) and `27-01-epic-foo` (new) as acceptable shapes `[manual]` | Story 3 | `[manual]` | ✓ |
