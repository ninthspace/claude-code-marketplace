# Coverage Matrix: Enforce Retro Generation & Application (do-centric)

**Source spec**: docs/specifications/35-spec-enforce-retros.md
**Epic**: docs/epics/35-01-epic-enforce-retros.md
**Date**: 2026-06-07

> **Verification rule**: Verification status (✓) is bound to criterion text. Any change to a story criterion or its spec mapping resets that row to unverified.

| # | Spec Requirement | Spec Text (verbatim) | Story Criterion (verbatim) | Covered by | Spec Test Approach | Verified |
|---|------------------|----------------------|----------------------------|------------|--------------------|----------|
| 1 | FR1 | `do` Step 8 produces the retro file at epic completion, via the shared synthesis procedure (AD1). | `do` Step 8 produces the retro file at epic completion via the shared synthesis procedure | Story 2 | `[manual]` | ✓ |
| 2 | FR2 | `do` records retro-trigger flags at the moment they occur during the loop: a verification gate resolved fail-then-continue, repeated `[tdd]` reds, test-command failures, blocked/stuck stories, an epic-level spec gap, or `**Inline change**` breadcrumbs. | `do` records retro-trigger flags at the moment they occur during the loop: a verification gate resolved fail-then-continue, repeated `[tdd]` reds, test-command failures, blocked/stuck stories, an epic-level spec gap, or `**Inline change**` breadcrumbs | Story 2 | `[manual]` | ✓ |
| 3 | FR3 | Generation is mandatory when the flag set is non-empty; auto-skip is permitted only when empty, and the skip is logged with its reason in the batch summary — never silent. | Generation is mandatory when the flag set is non-empty | Story 2 | `[manual]` | ✓ |
| 4 | FR3 | Generation is mandatory when the flag set is non-empty; auto-skip is permitted only when empty, and the skip is logged with its reason in the batch summary — never silent. | Auto-skip is permitted only when the flag set is empty, and the skip is logged with its reason in the batch summary — must NOT skip silently | Story 2 | `[manual]` | ✓ |
| 5 | FR4 | `do`'s startup Retro Check becomes a hard disposition gate over relevant prior-epic retro observations — each shown verbatim with its category, forcing Applied / Deferred-with-reason / Obsolete. A single acknowledgement must not satisfy it. | `do`'s startup Retro Check becomes a hard disposition gate over relevant prior-epic retro observations — each shown verbatim with its category, forcing Applied / Deferred-with-reason / Obsolete — must NOT resolve on a single acknowledgement | Story 3 | `[manual]` | ✓ |
| 6 | FR5 | Each disposition writes `**Retro applied**: {nn} · {category} · {disposition} — {note}` onto the epic being worked. | Each disposition writes `**Retro applied**: {nn} · {category} · {disposition} — {note}` onto the epic being worked | Story 3 | `[manual]` | ✓ |
| 7 | FR6 | Under autonomous execution the gate must not block: it auto-records `deferred (autonomous run, unreviewed)` and surfaces every such deferral in the run summary. Generation still fires under ralph. | Under autonomous execution the consumption gate does not block: it auto-records `deferred (autonomous run, unreviewed)` and surfaces every such deferral in the run summary — must NOT block the loop | Story 4 | `[manual]` | ✓ |
| 8 | FR6 | Under autonomous execution the gate must not block: it auto-records `deferred (autonomous run, unreviewed)` and surfaces every such deferral in the run summary. Generation still fires under ralph. | Retro generation still fires under ralph (non-interactive) | Story 4 | `[manual]` | ✓ |
| 9 | FR7 | `cpm2:ralph`'s gate-override table and its autonomous-behaviour prompt clause gain an explicit defer-and-log entry for the new gate (honouring the line-242 maintenance contract). | `cpm2:ralph`'s gate-override table and its autonomous-behaviour prompt clause gain an explicit defer-and-log entry for the new gate — must NOT leave the gate to the generic "most reasonable option" | Story 4 | `[manual]` | ✓ |
| 10 | FR8 | `do`'s batch/run summary reports the retro outcome (generated; or auto-skipped + reason; or deferred-unreviewed count under ralph). | `do`'s batch summary reports the retro outcome (generated; or auto-skipped + reason) | Story 2 | `[manual]` | ✓ |
| 11 | FR8 | `do`'s batch/run summary reports the retro outcome (generated; or auto-skipped + reason; or deferred-unreviewed count under ralph). | surfaces every such deferral in the run summary | Story 4 | `[manual]` | ✓ |
| 12 | AD1 | Extract the "collected observations → retro-file body, then write" core into a shared procedure in `cpm2/shared/skill-conventions.md`. Both `do` Step 8 and `/cpm2:retro` Steps 2–3 call it. | A shared **Retro Synthesis** procedure is added to `cpm2/shared/skill-conventions.md` … Both `do` Step 8 and `/cpm2:retro` reference the one shared procedure; no duplicated synthesis logic remains | Story 1 | `[manual]` | ✓ |
